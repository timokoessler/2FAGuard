<Page
    x:Class="TOTPTokenGuard.Views.Pages.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TOTPTokenGuard.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:totpui="clr-namespace:TOTPTokenGuard.Views.UIComponents"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
    Title="Home"
    d:Background="White"
    d:DesignHeight="700"
    d:DesignWidth="1150"
    mc:Ignorable="d">
    <Grid>
        <StackPanel x:Name="LoadingInfo" HorizontalAlignment="Center">
            <svgc:SvgViewbox
                Width="150"
                Margin="0,50,0,0"
                HorizontalAlignment="Center"
                Source="pack://application:,,,/Assets/padlock-gradient.svg" />
            <ui:TextBlock
                Margin="0,25,0,0"
                FontSize="26"
                VerticalAlignment="Center"
                FontWeight="SemiBold"
                Opacity="0.75"
                Text="{DynamicResource i.home.loading}"
                TextAlignment="Center" />
            <ProgressBar
                Margin="0,40,0,0"
                IsIndeterminate="True" />
        </StackPanel>
        <WrapPanel
            x:Name="TOTPTokenContainer"
            Margin="0,20,0,0"
            VerticalAlignment="Top"
            Orientation="Horizontal"
            Visibility="Collapsed"/>
    </Grid>
</Page>
