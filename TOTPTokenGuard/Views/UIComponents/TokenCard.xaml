<UserControl
    x:Class="TOTPTokenGuard.Views.UIComponents.TokenCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TOTPTokenGuard.Views.UIComponents"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="350"
    Height="150"
    d:Background="White"
    mc:Ignorable="d">
    <Border
        x:Name="Card"
        Margin="10"
        CornerRadius="10">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource CardBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
                <Setter Property="BorderThickness" Value="{DynamicResource CardBorderThemeThickness}" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource CardBackgroundPointerOver}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Image
                Width="45"
                Margin="24,0,0,0"
                Source="pack://application:,,,/Assets/key-256.png" />

            <StackPanel
                Grid.Column="1"
                Margin="24"
                VerticalAlignment="Center">
                <ui:TextBlock
                    Margin="0"
                    FontSize="18"
                    FontTypography="BodyStrong"
                    Text="Google"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBlock
                    Appearance="Secondary"
                    Text="info@example.com"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBlock
                    FontSize="32"
                    FontWeight="Bold"
                    Text="123 456" />
            </StackPanel>
            <StackPanel
                Grid.Column="2"
                Margin="24"
                VerticalAlignment="Center">
                <ui:Button Icon="{ui:SymbolIcon Copy24, Filled=True}" />
                <ui:Button Margin="0,10,0,0" Icon="{ui:SymbolIcon Edit24, Filled=True}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
